Change sim params or strategy used.

Run run.py. This will create json with sim data.

Run a server via terminal command python -m http.server 8000

You can now navigate to localhost:8000/percentiles.html (or any other html page)
Server is required for the pages to load the json data.